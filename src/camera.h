#ifndef CODE_TEMPLATE_CAMERA_H
#define CODE_TEMPLATE_CAMERA_H
#include <vector>
#include <cmath>
#include "parser.h"

std::vector<parser::Vec3f> get_directions(const parser::Camera &cam);

#endif
